# Google Gemini API key — get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# System prompt for the portfolio terminal assistant.
# Paste the full prompt as a single-line value (use \n for line breaks if needed).
# If this variable is not set, a generic fallback is used.
GEMINI_SYSTEM_PROMPT=
